# Copy to .env beside docker-compose.yml. Never commit the real file.
# Generate each token with:  openssl rand -base64 36

# The same value as deploy/typesense/.env. Two stacks, one shared secret: change it in both and restart both.
# It never leaves the VPS — only this service holds it, and only this service can reach Typesense.
TYPESENSE_ADMIN_KEY=

# The web app's token. Reading only — it cannot import documents or drop a collection. This is the one that goes
# on Vercel, where a leak should be able to do as little as possible.
SEARCH_API_TOKEN=

# The worker's token, for `cli sync:typesense` and the daily sync workflow. Reads and writes.
# Must differ from SEARCH_API_TOKEN; the service refuses to start if they match.
SEARCH_API_ADMIN_TOKEN=

# --- The deck crawls (T036). Leave all three blank and /cron/:source answers 503; search is unaffected. ---
#
# The Supabase project URL, https://<ref>.supabase.co — Project Settings → Data API → Project URL.
# SUPABASE_URL=
#
# The service-role (secret) key, Project Settings → API keys. The web app calls this same value SUPABASE_SECRET_KEY;
# the Go service reads it under the name below. It bypasses RLS across the whole database, so it stays on the VPS
# and nowhere else — never on Vercel, never in the browser.
# SUPABASE_SERVICE_ROLE_KEY=
#
# The token the web app's cron route sends here. A third token, doing less than either of the two above: it can
# trigger a scrape and read a crawl's status, nothing else. Generate with `openssl rand -base64 36` and set the
# identical value on Vercel as SEARCH_API_CRON_TOKEN.
# SEARCH_API_CRON_TOKEN=

# The shared Docker network, created by the Typesense stack — deploy that one first. Set both of these, in both
# .env files, to use a network neither stack owns (a panel's shared network).
# SEARCH_NETWORK=mtg-search
# SEARCH_NETWORK_EXTERNAL=true

# The **host** port, on loopback. Change this if something else on the VPS already has 8090 — this is the only
# port here that can collide with anything.
# SEARCH_API_PORT=8090

# The port **inside** the container. A container has its own network namespace, so this cannot conflict with a host
# service or with another container, whatever either of them uses; it is here for tidiness, not for conflicts.
# If you change it, the Traefik label and the health probe follow automatically.
# SEARCH_API_CONTAINER_PORT=8080

# --- Only when Traefik in Docker is the reverse proxy ---
# Leave all of this commented out if nginx or Caddy runs on the host: it reaches this service through the loopback
# port above, and none of these apply.
#
# Traefik cannot use that loopback port. It discovers containers by label and connects to them container to
# container, so it needs both of the first two set — the labels alone do nothing without the shared network.
# TRAEFIK_ENABLE=true
# TRAEFIK_EXTERNAL=true
# The hostname Traefik should route here.
# SEARCH_API_HOST=search.example.com
# Traefik's Docker network, its entrypoint and its ACME resolver, if yours are not named these.
# TRAEFIK_NETWORK=traefik
# TRAEFIK_ENTRYPOINT=websecure
# TRAEFIK_CERT_RESOLVER=letsencrypt

# Set to a registry tag if you build the image elsewhere rather than on the VPS.
# SEARCH_API_IMAGE=ghcr.io/planeswalker-industries/mtg-search-api:latest
